<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            {#
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{ path('main_todo') }}">Todo</a>
            </li>
            #}
        {% if not app.user %}
            <li class="nav-item">
                <a class="nav-link active menuItem" aria-current="page" href="{{ path('app_login') }}" title="Login">{{ 'connection' | trans }}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{ path('app_register') }}" title="Register">{{ 'registration' | trans }}</a>
            </li>
        {% else %}
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{ path('main_home') }}" title="Home">{{ 'home' | trans }}</a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false" title="Profil">
                    {{ 'profil' | trans }}
                </a>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <li><a class="dropdown-item sousMenu" href="{{ path('fiche_list', {'id':app.user.id}) }}">Fiches de personnage</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item sousMenu" href="{{ path('profil', {'id':app.user.id}) }}">{{ 'profil' | trans }}</a></li>

                </ul>
            </li>
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{ path('app_logout') }}" title="Logout">{{ 'logout' | trans }}({{ app.user.pseudo }})</a>
            </li>
        {% endif %}
        </ul>
    </div>
</nav>




